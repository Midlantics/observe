openai>=1.0.0
httpx>=0.27.0
python-dotenv>=1.0.0

# Optional — only needed for llm_providers.py demos:
# pip install anthropic              (for demo_anthropic)
# pip install google-generativeai    (for demo_gemini)
